name: Refresh hub data

# Manual compatibility entry point. The deployment workflow also runs twice daily.
# The VM remains the sole writer of the live release history.
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/deploy.yml
